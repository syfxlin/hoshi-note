spring:
  application:
    name: hoshi-note
  main:
    cloud-platform: kubernetes
    allow-circular-references: true
  cloud:
    kubernetes:
      client:
        namespace: ${KUBERNETES_NAMESPACE:hoshi-note}
