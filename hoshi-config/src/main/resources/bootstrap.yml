#
# Copyright (c) 2021, Otstar Lin (syfxlin@gmail.com). All Rights Reserved.
#
server:
  port: 0
spring:
  application:
    name: hoshi-config
  cloud:
    config:
      server:
        git:
          uri: file:///${user.dir}/static/config
  security:
    user:
      name: hoshi-config
      password: 123456
eureka:
  instance:
    instance-id: ${spring.application.name}:${random.int[1,100]}
  client:
    service-url:
      defaultZone: http://hoshi-peer1:7001/eureka/,http://hoshi-peer2:7002/eureka/
encrypt:
  key-store:
    location: classpath:/key.jks
    alias: hoshi-key
    password: kudgJi8jj5adWu
    secret: kudgJi8jj5adWu
