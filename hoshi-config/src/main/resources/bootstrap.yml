server:
  port: 0
spring:
  profiles:
    active: native
  application:
    name: hoshi-config
  cloud:
    config:
      server:
        native:
          search-locations: file:///${user.dir}/static/config
  security:
    user:
      name: hoshi-config
      password: 123456
eureka:
  instance:
    instance-id: ${spring.application.name}:${random.int[1,100]}
    metadata-map:
      user.name: ${spring.security.user.name}
      user.password: ${spring.security.user.password}
  client:
    service-url:
      defaultZone: http://hoshi-peer1:7001/eureka/
encrypt:
  key-store:
    location: classpath:/key.jks
    alias: hoshi-key
    password: kudgJi8jj5adWu
    secret: kudgJi8jj5adWu
management:
  endpoints:
    web:
      exposure:
        include: "*"
