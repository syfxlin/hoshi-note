apiVersion: apps/v1
kind: Deployment
metadata:
  name: hoshi-registry
  labels:
    app: hoshi-registry
spec:
  replicas: 1
  template:
    metadata:
      name: hoshi-registry
      labels:
        app: hoshi-registry
    spec:
      containers:
        - name: hoshi-registry
          image: ghcr.io/syfxlin/hoshi-note:registry
          imagePullPolicy: IfNotPresent
          command:
            - java
            - -jar
            - /app/hoshi-registry-1.0-SNAPSHOT.jar
          ports:
            - name: hoshi-registry
              containerPort: 7500
              hostPort: 7500
      restartPolicy: Always
  selector:
    matchLabels:
      app: hoshi-registry
